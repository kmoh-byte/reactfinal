export default function MyInput() {
    let content = "some content";
    return(
    <>

    <p>{content}</p>
    <input type="text" />

    </>
    );
  }
//Default Export for only exporting, only single component
// Named Export -> EXPORT MORE THAN ONE 

